const domain = 'http://localhost:8080';
var base = domain + '/api';
const api = {
    domain: domain,
    base: base,
    adminLogin: `${base}/admin/login`,
    personalDetail: `${base}/admin/detail`,
}